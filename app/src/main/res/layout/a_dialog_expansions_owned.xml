<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/dialog_screen_bg"
    android:orientation="vertical"
    android:padding="@dimen/activity_double_margin">

    <TextView
        style="@style/ArkhamTitle"
        android:id="@+id/expansions_owned"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:text="@string/expansions_owned" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvExpansions"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/activity_vertical_margin"
        app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
        tools:listitem="@layout/item_expansion_owned" />

    <Space
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:minHeight="@dimen/activity_vertical_margin"/>

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <Button
            android:id="@+id/cancel_button"
            style="@style/ArkhamButton.VerySmall"
            android:layout_gravity="start"
            android:text="@string/cancel" />

        <Button
            android:id="@+id/okay_button"
            style="@style/ArkhamButton.VerySmall"
            android:layout_gravity="end"
            android:text="@string/okay" />

    </FrameLayout>

</LinearLayout>